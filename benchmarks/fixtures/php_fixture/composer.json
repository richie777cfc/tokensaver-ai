{
  "name": "php-fixture/demo",
  "description": "Minimal PHP backend fixture for TokenSaver benchmarks",
  "require": {
    "php": ">=8.1",
    "vlucas/phpdotenv": "^5.5"
  },
  "scripts": {
    "serve": "php -S localhost:8000 -t public",
    "test": "phpunit"
  }
}
