{
  "name": "node-fixture",
  "version": "1.0.0",
  "description": "Minimal Node.js backend fixture for TokenSaver benchmarks",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "echo 'no tests configured'",
    "lint": "echo 'no linter configured'"
  },
  "dependencies": {
    "express": "^4.18.0",
    "dotenv": "^16.0.0"
  }
}
